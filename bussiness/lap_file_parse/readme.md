统一解析要求：
    1、用户上传模板，然后从中解析标题，进行数据提取
    2、日期统一改为 2025/01/07

1、格式1
     a、csv 文件只会出现两部分，就是上下两个子表，上下两个子表可以进行左右两侧链接【这个会不会出现三部分？】
     b、 上下两个子表，按行匹配是按照 sid + 日期进行匹配，不会重复
     c、 上下两个表是行与行之间相互匹配对应的，及时不适用 sid 加日期，也是一一对应的
     d、 文件都需要是 csv 格式的
     =================================================
     [1] SID 需要删除后缀 011
     --------------------------------------【问题】
     [2] 参数数值列左侧一定有一列空着或者是位置列, 该列是位置列 ????
     [2] 模板中的 CO2 需要把下标给处理下，我这边直接读取 excel 再读取识别这个下标是比较困难的，希望改成 CO2 ????
 

2、格式2
   =================================================
   1、SID 需要去除掉 -011 后缀

    

3、生化电解质
        
        传递的方式
           1+3， 2
           1+2, 3
           1,2
        字段映射问题
    }

   =================================================
   1、获取的事是 PID
   2、id 的缩写格式就是 .1.0324.2.343
   注意各个文件的识别标识符
    样式一：



2 血常规
    a. 首行 ID 不会重复
    b. 模板中的字段和模板中的字段不符合，这个该怎么处理？
    是样式中不仅有模板中的，而且有
    c. 文件名字不能有逗号，因为是按照逗号分割的
    d。 类别1 和 类别2 的 sheet 名字问题. [sheet1]
    





3 临床免疫的要求
    [a]: alarm 的列必须是以 _Alm 结尾
    [b]: 列到右侧属于一个范围 【302 到 574 中间的都属于 302 的范围
        302						                    574				
        Value	V_Unit	D_Alm	R_Msg	Dil	Module	Value	V_Unit	D_Alm	R_Msg	Dil
    模板行数需要固定？？

4 血液凝固
            "P.C": "pc",    在文件中是 P.C 还是 P/C???


5、糖化血红蛋白 
    问题一个： 这个编号是怎么算的？这个的话就是组编号吗？我们就不用处理了
    D-7-A0306335
    D-7-A1102297
    D-7-A1106031
    D-7-A1000043

6、尿常规
    1、数据是否也需要删除 PC 开头的是否需要删除 ？？？？？？？？

总体要求
    1. 不能传递重名的文件，比如 ：同时上传两份一样的文件，名称都但是 样品1.xlsx
    2. 同一种类的文件，能不能格式都是一样的？比如电解质当中，都是 csv, 不要有的是 csv 有的是 excel
 

